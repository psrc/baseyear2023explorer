prcltzbl.csv file created using the script dbf2csv.py in ~/src/python/scripts
    - original dbf file in DataTeam/Projects/2018_base_year/Region/May2020/
    
Run prepare_parcels_geo.R to convert the prcltzbl.csv into an rds file.

Run convert_datasets.R to convert the remaining datasets from csv into rds. 
